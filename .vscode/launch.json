{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ Project",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/app",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "lldb"
        }
    ]
}